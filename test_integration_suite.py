)e 1lsif success e0 t(s.exi
    sytests()n_ntegratioun_is = r    succes":
__main__ == "if __name__


scesn sucur  ret
  い。")
    。詳細を確認してくださのテストが失敗しました 一部nt(f"\n⚠️  pri       else:
  ")
 ています能は正常に動作しディタの統合機✨ フラクタルエprint(f"   
     ました！")が成功し統合テスト"\n🎉 すべてのt(f   prin
     ss:cce 
    if sus) == 0
   errorlen(result.s) == 0 and ult.failure = len(res    success

    ))[-2]}"('\\n'itaceback.spl: {tr"  - {test} print(f        rrors:
   in result.eraceback  tst, for te       
したテスト:") エラーが発生\n💥(f" print
       rors: result.er if)
    
   \\n')[0]}"1].split(': ')[-Errorrtiont('Asseeback.splitracest}: {- {t(f"  print      
      ures:esult.faileback in rest, trac t      for:")
  ❌ 失敗したテスト\nnt(f"       pris:
 reiluult.fa
    if res
    errors)}")lt.{len(resu  エラー: int(f"")
    pres)}ur(result.fail"  失敗: {lent(f  prins)}")
  rrorult.en(reses) - leilur.fa len(resulttsRun -ult.tes"  成功: {resnt(f  pri)
  stsRun}" {result.te実行テスト数:print(f"  ")
    ー:テスト結果サマリ統合📊 f"\nt(   prinーを表示
 果サマリ  # 結e)
    
  est_suit.run(t = runner  resultstdout)
  ream=sys.=2, stverbositynner(.TextTestRustr = unitte  runne成して実行
  ンナーを作ストラ テ   
    #n))
 grationteequirementsIstR(Test.makeSuite(unitte.addTestt_suite
    tes検証テスト要件統合
    #    tion))
 nceIntegraPerformaestSuite(Tt.makest(unittessuite.addTet_  tesーマンステスト
  # パフォ
    
    dWorkflow))dToEnte(TestEntest.makeSui(unitTest.add_suite    test
ストーテエンドワークフロツー    # エンド  
))
  rationIntegTestUIModelmakeSuite(est.ttdTest(uniadsuite.test_統合テスト
    -モデル# UI  
    e()
  estSuit.T = unittestite test_su作成
    テストスイートを    #)
    
を開始します\n"統合テストスイートエディタクタルnt("🚀 フラ   pri行"""
 トスイートを実""統合テス):
    "sts(egration_tef run_int


de統合検証成功")フェースのユーザーインター要件5:  ✅ t("     prin
    lose()ow.cnd main_wi            
  50)
 , 1ationsterefault_max_itings.daded_setsertEqual(lo   self.as    gs()
 tinad_setmanager.los_ttingngs = seaded_settilo       
 
        cess)(save_sucertTrue.ass      selfs)
  _settingettings(testr.save_sngs_manage setticcess =   save_su     

        
        )8)=(1024, 76_image_sizeult   defa        
 tions=150,iteramax_efault_    d      
  gs( = AppSettingstest_settin 
             )
         json")
 st_settings.ui_teir, ".temp_dn(selfath.joi     os.p
       gsManager(er = Settinttings_manag se能
        5.5: 設定保存機
        #))
        00, 50ffset'), (1e('pan_o_stater.get_uintrollcoqual(ui_.assertE        self 2.0)
evel'),te('zoom_li_star.get_ui_controlleertEqual(u.ass    self 
          0, 50))
 fset', (10'pan_of_state(ler.set_ui  ui_control      el', 2.0)
e('zoom_lev.set_ui_statrollernt   ui_co     ーション
シミュレパラメータ変更の#           
  )
    initialize(troller.ui_con     er()
    UIControllntroller = ui_co能
       .4: プレビュー更新機       # 5      
 ea)
  splay_ar_difractalone(otNsertIsN self.as()
       y_areatal_displaw.get_frac= main_windosplay_area   fractal_di
      （モック）, 5.3: マウス操作.2   # 5        
    768)
  ght(),imumHeiinndow.mual(main_witGreaterEq self.asser   1024)
    , h()minimumWidtn_window.erEqual(maiatlf.assertGre        se)
フラクタルエディタ"), "tle(indowTiow.w_windainsertEqual(m self.as     ow)
  n_windaisNotNone(mertI   self.ass    確認
 されることを初期化ィンドウが正しく   # ウ 
     
       w() MainWindodow =in_win        maンウィンドウの表示
: メイ       # 5.1        
 ..")
の統合検証を実行中.ンターフェース: ユーザーイnt("  🔄 要件5   pri""
     スの統合検証"ンターフェーーイ"要件5: ユーザ  ""     f):
 rface(sel_5_user_inte_requirementtest
    def    成功")
 統合検証像出力機能の✅ 要件4: 画int("      pr    
      ズ
  2倍のサイ)  #  200)e, (200,es_img.sizal(hirtEquf.asser  sel     
     g:ires_im as hes_path)en(hirh Image.op     witpath))
   es_hirxists(path.eTrue(os.ert   self.ass   
      )
      2
      or=factue, scale_on=Trsoluti, high_re_path  hires         
 erations,ax_ita, params.meration_datlt.it     resuge(
       rt_imaengine.expoendering_ r       ort.png")
 "hires_expp_dir,f.temeln(s.path.joi ospath =hires_力
        像度指定と高解像度出.4, 4.5: 解
        # 4      t)
  ected_formaexpmat, qual(img.forf.assertEsel                 "JPEG"
   " else == "pngmat_ext PNG" if format = "xpected_for e                 img:
   th) asort_pa(expe.openmag    with I        ことを確認
    存される形式で保 # 正しい              
             ath))
    ts(export_pth.exisos.paertTrue(.asself     s           とを確認
が作成されるこイル# ファ               
                )
                    h
 xport_pat   e               s,
      ionteratparams.max_ia, ration_datte  result.i                      (
ageport_imgine.exndering_en     re          
          else:                )
               =90
th, qualityxport_pa      e                  ns,
iteratios.max_am, parn_datatiotera result.i                    (
   ageport_ime.ex_enginendering     r               :
"jpg"t == _exif format               
       
          , filename)irself.temp_dh.join(os.patport_path =  ex        
       _ext):t=formatest(formalf.subTseh     wit       
 ormats: in export_ffilenamet, at_exfor form         
    ]
     
      ")jpgest_export.", "t  ("jpg       ),
   export.png"test_("png", "           ts = [
 port_forma   ex
     力式での出G形: PNG/JPE 4.2, 4.3# 4.1,
        
        mapper)pper(color__color_masetngine.g_eenderin   r  e()
   gEnginderinengine = Renring_de     ren    
   r()
    olor_mapper.get_color_manage cor_mapper =       colnbow")
 ("Raiby_name_palette_enturr_cager.setman     color_  )
 mManager( ColorSysteanager =or_m   colジンを設定
     エン   # レンダリング     
     rams)
   calculate(paenerator.lt = g        resu     )
}
   ameters={om_parst        cu),
    0, 100_size=(10ge      ima=50,
      _iterations max            ),
     
      0)er(1.0, -1.umbComplexNght=_ri   bottom        1.0),
     umber(-2.0, omplexNeft=C_lop      t
          egion(on=ComplexR  regi
          s(eteralParams = Fract  param      nerator()
ndelbrotGeMa = enerator成
        gルデータを生 # フラクタ     
        .")
  実行中..合検証を機能の統 画像出力 要件4:  🔄int("      pr"""
  能の統合検証: 画像出力機"""要件4  
      rt(self):mage_expont_4_iremerequiest_ef t 
    d  功")
 機能の統合検証成・視覚効果要件3: 色彩int("  ✅  pr 
              age)
, Image.Imced_imagehannstance(ensIf.assertI   sel  
       
      )ings
      anced_settnhns, eratioite params.max_tion_data,rate.i    result       iew(
 revne.render_pgiering_ennd= reanced_image         enh    
     )
    1.1
   ntrast=         co
   tness=1.2,gh         bri
   sing=True,   anti_alia        ings(
 ettrSndetings = Reanced_set      enhコントラスト調整
  輝度・シングと アンチエイリア4, 3.5:     # 3.      
 mage)
    age, Image.Icustom_imstance(sertIsIn    self.as        
 
     )ons
      ratiitearams.max_, p_datarationlt.ite        resu
    r_preview(degine.rening_en = renderage custom_im
       )
        perm_mapper(custocolor_mapt_ngine.seing_e    render    _palette)
tomcuser(rMappradientColoapper = Gom_m      cust
   )
              )]
 5) 0, 25, (1.0, (0, 0)).5, (0, 255,0, 0)), (0.0, (255,       [(0     ット",
    "テストパレ       te(
  om_paletste_cuager.creatan_me = colorstom_palett
        cuカラーマッピング: カスタム       # 3.3     
  50))
   size, (50,ge.ual(imassertEq    self.a          age)
  e.Im Imag(image,InstancetIssserself.a              
                       )
         ns
  tioms.max_iteraa, paradateration_.it  result          
        r_preview(gine.rendeering_en= rend    image                
       er)
      (color_mappperapset_color_mengine.ndering_      re
          apper()olor_mr.get_clor_managemapper = co   color_      )
       _namette(paleamepalette_by_nrent_.set_curmanager  color_            
  e):ame_nlette=palett.subTest(paelf     with s:
       ble_palettesn availame ir palette_na
        fo"]
        eyscal", "Gra "OceanFire", ""Rainbow",ettes = [vailable_pal   aン
     ト選択とグラデーショーパレッ.2: カラ3.1, 3      #      
  e()
   gEngin= Renderinne ndering_engire   
     temManager()orSysr = Colcolor_manage化
        テムを初期# 色彩シス
        
        ms)lculate(paraator.ca = gener    result
           )
 ters={}mera_patom cus         
  e=(50, 50),age_sizim      ,
      s=50iteration     max_      ),
   
          .0)mber(1.0, -1lexNut=Comp bottom_righ           
    .0, 1.0),mber(-2Nueft=Complex       top_l
         ion(xRegon=Comple     regi   ters(
    ractalParamems = Fara   p   ()
  atortGenerdelbroMan=  generator       を生成
 クタルデータ# フラ            
  ...")
  検証を実行中果機能の統合視覚効3: 色彩・int("  🔄 要件      pr
  証"""彩・視覚効果機能の統合検3: 色  """要件      lf):
l_effects(seor_visua3_colt_remenrequief test_
    d)
    検証成功"メータ調整機能の統合✅ 要件2: パラ"    print(      
     )
          "
 映される必要があります更が結果に反回数の変       "反復
     e,  Tru          ),
eration_datalt.itsu_reied modifration_data,e_result.ite(basrray_equal.a   np
         l(qua.assertNotElfse       詳細レベルの変化）
 異なることを確認（ # 結果が
             
  )ied_paramste(modifulator.calct = generaied_resulifod     m
   arams)(base_plater.calcu = generatoe_result    bas
            )
   rs
     arameteom_pcuste_params.rameters=bas_pastom        cu   _size,
 mages.i_parame_size=base        imag
    =100,  # 変更nsiteratioax_    m  
      .region,msase_paraon=bregi           rameters(
 actalPaparams = Frfied_        modiト
 反復回数変更テス        #        
)
        ={}
rametersstom_pa    cu       ),
 (50, 50ze=    image_si     ns=50,
   ratio     max_ite),
                 .0)
  0, -1er(1.omplexNumb=C_right   bottom            0),
 , 1.-2.0lexNumber(ft=Comp     top_le      on(
     =ComplexRegi    region(
        ersarametalP= Fractparams se_   baト
     更のテスタ変パラメー 各種2.4, 2.5:, .3    # 2.2, 2  
       あります")
   義が提供される必要がメータ定  "パラ                   
     ons), 0, efinitirameter_dter(len(pasertGrea    self.asns()
    efinitioameter_dor.get_pareneratnitions = ger_defi   paramet
     整UIの表示（モック）1: パラメータ調 # 2.     
          rator()
tGenebro= Mandelr   generato
             ")
 の統合検証を実行中...調整機能🔄 要件2: パラメータ"        print("
  機能の統合検証""パラメータ調整"""要件2:        
 ent(self):er_adjustmarametrement_2_pquitest_re
    def 
    ")統合検証成功クタル生成機能の✅ 要件1: フラ print("  
              , 100))
 100hape, (_data.sionsult.iteratl(julia_resertEqua.as      self  talResult)
sult, Fraclia_rejunce(Insta.assertIs     self)
   _paramslate(juliacu = julia.cal_result   julia          
   )
  
      27015)}0.7, 0.exNumber(-Compl'c': eters={ustom_param        c
    100),size=(100,    image_     =100,
    iterations        max_,
     )      
     , -1.5).5plexNumber(1ht=Comottom_rig         b     1.5),
  5, Number(-1.omplex  top_left=C        on(
      gilexRegion=Comp       reters(
     arames = FractalP julia_param    a']
   liors['jule_generatavailab   julia = 的正確性
     数学合のュリア集# 1.3: ジ  
        )
      (100, 100)a.shape, ration_datt_result.itendelbroEqual(malf.assert se       t)
esullR Fractabrot_result,elandIsInstance(mf.assert      sel
  ams)culate(parelbrot.calnd= mault brot_res mandel   
                    )
eters={}
ustom_param  c          100),
size=(100, mage_     i
       ations=100,er      max_it ),
                 )
-1.0.0, mber(1ht=ComplexNuottom_rig  b            
  .0),(-2.0, 1mberexNueft=Compl    top_l       
     gion(n=ComplexReio        reg
    (arameterslPracta params = F      rot']
 s['mandelbatore_generailabldelbrot = av man
       正確性デルブロ集合の数学的 マン     # 1.2:
         があります")
  が利用可能である必要ラクタルタイプ要求される3つのフ  "                
        3,s),eneratore_glabll(len(avaiualf.assertEq   se             
       }

 タム"), "カス*2 + c""z*r(ratoGenestomFormulatom': Cu  'cus     (),
     eratora': JuliaGen       'julir(),
     GeneratondelbrotMa: ndelbrot'ma   '{
         erators = vailable_gen a選択
       プの表示とタイ利用可能なフラクタル, 1.4, 1.5:  # 1.1       
        
")合検証を実行中...成機能の統 要件1: フラクタル生"  🔄nt(pri   
     合検証"""成機能の統1: フラクタル生  """要件):
      elfion(sl_generat_1_fractarementequit_r   def tes
    
 =True)nore_errors, igtemp_dire(self.il.rmtre     shututil
      import sh
     """各テストの後処理     """
   self):n( def tearDow    
   dtemp()
pfile.mkp_dir = tem.temself"
        """"各テストの前準備"   
     self):tUp(f se  de   
  
 ance()ston.in QApplicatipp =ls.a   c     
        else:    (sys.argv)
ication QApplpp = cls.a           ):
nstance(ion.iot QApplicatif n
        """初期化テストクラス全体の    """s):
    pClass(cldef setU   od
 thlassme 
    @c"""
   全要件の統合検証テスト
    """TestCase):test.n(unittegratioirementsIns TestRequclas")


フォーマンステスト成功ダリングパnt("  ✅ レン   pri  
     )
       (400, 400)size,(image.rtEqual  self.asse           e)
   age.Image(image, ImIsInstancassert self.              確認
 れることを画像が正しく生成さ  #            
                  す")
 すぎまレンダリング時間が長ription}の f"{desc                             5.0, 
  me,render_tis(Lesrtlf.asse    se     を確認
       であることが合理的ダリング時間      # レン  
                      
  .3f}秒")e:_tim}: {renderionescript   {drint(f"   p               
          
     timeart_ st() - = time.timeme  render_ti             
       )      s
    ettings, son.max_iteratiarametersn_data, pteratio  result.i                 _preview(
 renderng_engine.enderiimage = r            time()
     = time.start_time          
              r)
        _mappeer(color_color_mappe.setgindering_en  ren        r()
      or_mappeoler.get_cr_manager = colo_mapp    color            )
ow""Rainbname(palette_by_set_current_ger.  color_mana           on):
   escriptisettings=d.subTest(lfth se        wings:
    tti_seion in testcriptettings, desfor s  
           ]
          ")
 1), "フル設定contrast=1.tness=1.2,  brigh=True,inganti_aliass(Settingerend      (R),
      イリアシング有効" "アンチエsing=True),(anti_aliaingsRenderSett      ("),
      イリアシング無効e), "アンチエliasing=Falsanti_aings((RenderSett      [
       tings =_settest
        測定間を定でのレンダリング時る設異な    #    
    
     gEngine()ine = Rendergin_enrendering  ()
      emManagerColorSystanager = _m  color     
 設定レンダリングエンジンを # 
       )
        ters(parameculate.cal generatorresult =       
      
     )      rs={}
rametem_pa  custo    
      400),0, e=(40  image_siz
          ns=100,iteratio max_       ),
               0)
 1.0, -1.mplexNumber(ght=Com_ri botto           .0),
    er(-2.0, 1lexNumbp_left=Comp        to       Region(
 lexgion=Comp     re      rs(
 mete FractalPara =rs    paramete
    ()atorotGenerlbrManderator = ne ge       タルデータを生成
ラク # フ             
  実行中...")
フォーマンステストを 🔄 レンダリングパt("         prinステスト"""
フォーマン""レンダリングパ       "ce(self):
 ng_performanest_renderief t    
    d
ンステスト成功")パフォーマ✅ 並行計算int("      pr    
    ape)
    tion_data.sheralt.it_resuarallel  p                 
      .shape,ataion_dt.iteratresulequential_ertEqual(slf.ass   se   確認
  あることを果が同じで   # 結        
")
     3f}秒llel_time:. 並行処理: {parant(f"   pri        .3f}秒")
ial_time:quent {seケンシャル:   シー" print(f)
        時間比較:"  📊 計算f"nt(     pri       
   tart_time
 me() - s time.time =arallel_ti       p
 rs)ramete(paulateator.calcsult = generel_reparall     のみ
   間測定雑なので、ここでは時並行計算は複 実際の      #
  time()ime. tart_time =
        st行計算（モック実装）        # 並

        _timeme() - start time.ti_time =tialensequ)
        metersralate(par.calcuneratoult = gel_resentia      sequ
  ime.time()me = tti  start_
      ーケンシャル計算       # シ     
          )
 ={}
 tersmecustom_para        ,
    300)_size=(300, age         im   tions=100,
ax_itera           m  ),
          0)
 , -1.umber(1.0lexNght=Comp   bottom_ri             1.0),
 (-2.0,exNumber_left=Compl     top     
      Region(mplex region=Co           (
alParameters= Fractarameters      pr()
   rotGenerato= Mandelbenerator 
        g測定間をーケンシャル計算の時        # シ
        
Service()ulationlcllelCa Parace =allel_servipar   スを初期化
     行計算サービ # 並     
      e
    ServicalculationllelCort Paraculator impallel_calvices.par_editor.serom fractalfr
             ...")
   ォーマンステストを実行中算パフ 並行計t("  🔄        prinト"""
ォーマンステス"並行計算パフ""
        e(self):_performanculationcurrent_calcon test_c 
    def
   ト成功")フォーマンステス✅ メモリ使用量パnt("  ri
        p       能性）")
 ぎます（メモリリークの可増加が大きす使用量のメモリ      "                
 .0, ease, 100ncrss(memory_iertLe    self.ass内）
    範囲理的なことを確認（増加量が合メモリリークがない     #      
    MB")
   ase:.1f}ncreory_i 増加: {mem"   (f      print
  :.1f} MB")nal_memory終: {fi  最print(f"     )
     f} MB"ry_usage:.1{max_memof"    最大:     print(B")
    } M.1fy:nitial_memor{if"    初期: print()
        リ使用量:" 📊 メモprint(f" 
               al_memory
 ory - initieme = final_mmory_increas     me# MB
   / 1024  4  / 102_info().rssess.memoryprocry = memo      final_モリ使用量
    # 最終メ  
         
   gc.collect()           esult
     del r   
     果を削除してメモリを解放      # 結
              
    ry)_memo currentry_usage,memox(max_ge = mary_usax_memo ma    B
       # M 1024   1024 /s /nfo().rsry_irocess.memot_memory = pcurren      確認
      # メモリ使用量を       
               s)
  te(parameterulaator.calc gener   result =      
            )
               ={}
parameters    custom_       
      600),0,(60_size=ge  ima            s=200,
  eration      max_it                 ),
      0)
   (1.0, -1.bermplexNumht=Cobottom_rig             
       0, 1.0),2.plexNumber(-=Comop_left    t               
 Region(n=Complex     regio           ameters(
 FractalParers =paramet    
        n range(5):r i ifo        
   
     emory = initial_mory_usagememx_
        marator()elbrotGeneandr = Menerato
        gなフラクタルを複数回計算       # 大き       
 
 024  # MB 124 /().rss / 10.memory_infoess = proctial_memory   ini()
     til.Processess = psu proc記録
       量を 初期メモリ使用  #    
         ort gc
         imprt psutil
     impo     
  .")
    トを実行中..用量パフォーマンステスリ使("  🔄 メモ     print"
   "マンステスト"メモリ使用量パフォー  """lf):
      nce(se_performaageemory_us_mf test   de 
 功")
   スト成マンステフォーラクタル計算パint("  ✅ フpr
        
        s/sec")f} pixel]:.0r_second'_pet['pixels {resulf}秒,time']:.3result['"{          f
        : "olution']})ult['reses({ron']} ['descriptiult {resprint(f"             
  esults:nce_rrforman peult i   for res    マンス結果:")
 パフォーt("  📊    prin    示
 を表パフォーマンス結果  #    
      
     )度が遅すぎます"の処理速iption}cr  f"{des                             , 1000, 
  ondels_per_secGreater(pixert  self.ass             す")
 0秒を超えていまが3}の計算時間{description   f"                            0.0, 
, 3ion_timecalculats(esrtL.asse       self  
       ック件をチェォーマンス要  # 基本的なパフ                 
   
            })              per_second
d': pixels_seconls_per_     'pixe        me,
       tion_ti: calcula    'time'                pixels,
 ':  'pixels                ,
  eight}"{hdth}x f"{wiesolution':    'r            ion,
    descriptcription': 'des                 {
   d(lts.appenormance_resu perf               
        e
        culation_timcalixels / nd = pels_per_seco         pix    ht
   th * heigels = wid     pix    e
       _timme - startme = end_tin_tialculatio          c
                     )
 ime.time(d_time = t       en         ameters)
ulate(partor.calcgenerault = res               e.time()
 ime = timt_t  star         時間を測定
          # 計算                  

                 ){}
        _parameters=tomcus                ,
    ght)th, heiwid=(e_sizemag        i           
 0,tions=10_itera         max           ),
               1.0)
     er(1.0, -mplexNumbright=Com_      botto               
   , 1.0),r(-2.0mbelexNuop_left=Comp  t           
           plexRegion(=Comregion              
      rameters(ractalPars = F paramete         "):
      ght}x{hein=f"{width}ioTest(resoluth self.sub       wit     _cases:
in testription ht, descigth, hewid      for       
  ]
  sults = [rerformance_ pe  
     erator()brotGenMandelor = generat       
            ]
   "高解像度")
  , 00    (800, 6    "),
     "中解像度400,, (400          小解像度"),
  0, 100, "   (10        [
 s = st_case      teンステスト
  解像度でのパフォーマ  # 異なる
           
   を実行中...")ーマンステスト算パフォ"  🔄 フラクタル計nt(
        priステスト"""マンタル計算パフォーラク""フ"
        self):performance(alculation_ def test_c    
   e)
=Truors, ignore_errp_dir(self.temutil.rmtree        sh
rt shutilmpo
        iの後処理"""  """各テスト      lf):
tearDown(se
    def temp()
    .mkdmpfile = tep_dir self.tem"
       ""テストの前準備"""各        (self):
etUp def s"
    
   ""パフォーマンステスト    """ase):
stC.Teon(unittesttegratiformanceInTestPerclass )


功"ロー成式エクスポートワークフ ✅ 複数形nt(" 
        pri
        100))e, (100, al(img.siztEquf.asser       sel     
        ) as img:patht_oren(expage.op  with Im            めることを確認
  て読み込  # 画像とし               
        h))
       _patort(expth.exists(os.pa.assertTrue        self   を確認
     されること # ファイルが作成                    
            )
           
        ath    export_p                    erations,
it.max_parameters_data, t.iterationsul        re                t_image(
xporgine.eendering_en      self.r           se:
             el)
                        y=85
  path, qualit  export_                   ,
   ionsax_iteraters.met_data, paramation result.iter                     ge(
  xport_imaengine.eng_renderif.el        s            ":
jpg_name == "atorm  if f                  
      e)
      dir, filenammp_self.tein(= os.path.joh export_pat              name):
  format_Test(format=ubf.swith sel            t_formats:
e in exporame, filenormat_nam    for f       
     ]
   ,
     g")ort.jp, "test_exp"jpg"       (    t.png"),
 test_expor", "("png           ts = [
 export_forma   ト
     数形式でエクスポー     # 3. 複      
   r)
  r_mappeer(colo_color_mappseting_engine.self.render)
        per(et_color_mapor_manager.g= self.color_mapper     col   Plasma")
 "_by_name(ettecurrent_palset__manager.olor    self.cング設定
    . レンダリ # 2            
 ers)
  te(parametr.calculageneratosult = re       
        
   )}
      arameters={stom_p cu      00),
     e=(100, 1age_siz         im0,
   ions=10  max_iterat       
           ),
    .7, 0.1)lexNumber(-0ht=Comptom_rig      bot         0.2),
  .8,umber(-0mplexNop_left=Co    t            Region(
mplexon=Coregi          rs(
  ameteractalPar= Frs te  parame  
    ator()lbrotGenertor = Mande    genera   
 タルを生成    # 1. フラク      
.")
      トを実行中..フローテスクスポートワーク複数形式エ🔄 ("       print"
   トワークフロー""複数形式エクスポー     """
   flow(self):xport_workti_format_et_mul    def tes
")
    ワークフロー成功フラクタルム式nt("  ✅ カスタ       pri       
 
 0, 120))e, (12e.sizl(imag.assertEqua       selfe.Image)
 image, Imagtance(tIsInsasser   self.     結果を検証
リング   # レンダ  
     
      
        )tionsx_iterars.ma, parameteation_dataesult.iter   r(
         iewreve.render_ping_enginlf.render  image = se   
        mapper)
   lor_apper(coolor_mset_ce.ring_engin self.rende       )
per(apr_mologer.get_canalf.color_m_mapper = seolor  c   re")
   e("Fiette_by_namnt_palreer.set_curnag.color_ma       self4. レンダリング
 #         
        
))0, 120e, (12_data.shaptionresult.iterasertEqual(elf.as        ssult)
FractalRet, resulInstance(.assertIs        self結果を検証
        # 計算     
)
   rsate(parameter.calculeneratosult = g
        re算3. フラクタルを計
        #       )
   la}
       stom_formua': curmulameters={'fotom_par    cus   20),
     120, 1=( image_size           ons=60,
 max_iterati             ),
   0)
       r(1.0, -1.mbemplexNught=Cobottom_ri          .0),
      r(-1.0, 1ComplexNumbe   top_left=           ion(
  egn=ComplexRio     reg      
 ers(ctalParamet Frarameters = pa
       定 2. パラメータを設
        #       フラクタル")
 ula, "三次tom_formor(cusratneaGeFormulstomor = Cu   generatc"
     *3 + mula = "z*_for custom化
       器を初期カスタム式生成.    # 1      
 
      トを実行中...")ークフローテスタルワカスタム式フラク"  🔄    print(   ー"""
  ラクタルワークフロム式フカスタ""        "elf):
(sla_workflowustom_formut_c
    def tes 
   功")ークフロー成  ✅ ジュリア集合ワ("rint        p     
   
l")).fracta + "ject_pathsts(proos.path.exitTrue(lf.asser
        seを検証保存結果       #  
      
  ct_path)t, projeect(projecprojnager.save_ect_maoj  self.pr
      test")ulia_ "jmp_dir,lf.tepath.join(ses.t_path = o projec      
    eters
     aramrs = pct.paramete       proje
  "julia"pe =ractal_ty.fproject
        ")リア集合テスト("ジュt_projectreate_defauloject = c  pr保存
      ジェクトとして   # 5. プロ      
  0))
     0, 15e, (15image.sizsertEqual(    self.asImage)
    ge, Image.ce(imartIsInstanelf.asse
        sグ結果を検証ンダリン     # レ
    )
          ns
     eratioax_itarameters.m pion_data,erat result.it        view(
   nder_prengine.redering_elf.ren = se       image    
 per)
    or_mapmapper(colt_color_ine.seering_eng   self.rend    er()
 r_mapplot_cor_manager.geelf.coloper = solor_map c   )
    ean""Ocby_name(alette_et_current_pmanager.self.color_       sグ
 ッピングとレンダリン # 4. カラーマ 
       ))
       150, 150 (.shape,ion_datasult.iteratrtEqual(rese.as self    t)
   sulactalRe(result, FrncesInstaelf.assertI      s算結果を検証
   計     #    
   
    s)te(parametertor.calculanerault = ge
        resフラクタルを計算# 3.               
         )
 7015)}
(-0.7, 0.2lexNumber': Compters={'cmecustom_para     ,
       0), 15ze=(150age_si          im=80,
  ons max_iterati               ),
 
       , -1.5).5er(1exNumbt=Compl_righ    bottom            , 1.5),
-1.5mplexNumber(Co  top_left=           (
   Regionmplexegion=Co     r       rs(
Parameteactalers = Frmet   para設定
     有のパラメータを. ジュリア集合特        # 2  
   
   ()erator= JuliaGenrator     gene  
  ア集合生成器を初期化  # 1. ジュリ
          ...")
    を実行中ローテストリア集合ワークフ完全なジュint("  🔄 pr
        ロー"""なジュリア集合ワークフ"完全        ""lf):
flow(serk_julia_woeteest_compl  def t
    
  ")me:.3f}秒)culation_ti: {calクフロー成功 (計算時間ブロ集合ワー✅ マンデルrint(f"    p  
    
        _size)geefault_ima settings.dimage.size,(exported_assertEqualself.           
 :ed_image) as exportathport_page.open(ex   with Im
     path))sts(export_ath.exitTrue(os.pf.asser   sel  結果を検証
      # エクスポート           
    )
  path
     export_ons,terati.max_iametersn_data, parratio.iteltesu        r
    port_image(e.exering_enginf.rend    sel    png")
rot_export."mandelbdir, (self.temp_.path.join = ostht_papor      ex
  像をエクスポート 画0.       # 1  
 )
      delbrot"an "mractal_type,ject.floaded_proEqual(assertf.  sel")
      合テスト"マンデルブロ集ject.name, roal(loaded_pf.assertEqu selを検証
        # 読み込み結果         
   )
   al"+ ".fractct_path ject(projeoad_proanager.lct_melf.proje_project = s    loaded
    トを読み込みェク9. プロジ     #         
   ect_path)
roject, project(pr.save_projagect_man self.proje)
       ot_test"ndelbrdir, "maself.temp_.path.join(t_path = os      projeceters
  rammeters = pa.paraproject       トを保存
   # 8. プロジェク       
     )
  age_sizet_imefaulings.d, settze.sitEqual(imageelf.asser
        sage), Image.Imtance(imageassertIsIns self.     証
  グ結果を検ダリン レン       #
  )
         ons
      atiers.max_iter paramettion_data,.itera     result       (
_previewgine.renderg_enenderinge = self.r
        ima像をレンダリング  # 7. 画
      r)
        or_mappemapper(colset_color_gine.dering_en  self.ren      mapper()
or_et_colger.g_manaorself.colapper =  color_m   
    _palette)fault_colorgs.deinsettby_name(te_t_palett_currenager.seanlor_m.co      selfピングを設定
   6. カラーマッ
        #)
        time, 0calculation_ult.resater(ertGre    self.ass:-1])
    ize[:mage_sault_ings.defpe, settishaation_data.eritresult.assertEqual(lf.        seResult)
lt, Fractalstance(resusertIsIn self.as  果を検証
     # 計算結
           
     _timeme() - start time.ti =ation_timecul cal   ters)
    rameculate(paerator.cal genlt =esu        re()
.time = time start_tim計算
       5. フラクタルを       #      
 
    )      rs={}
 _parametetom   cus       
  mage_size,efault_iings.dize=settge_s        imaons,
    _iterati_max.defaultingsrations=settite      max_     ),
              -1.0)
ber(1.0,exNum=Complghttom_ri        bot        .0),
er(-2.0, 1plexNumbeft=Comp_l   to            on(
 exRegimpln=Coegio       r(
     Parametersactalters = Fr  parame  定
    4. パラメータを設 #             
 erator()
  rotGenandelbenerator = M      g  クタル生成器を初期化
     # 3. フラ    
   ")
    ストブロ集合テ"マンデルt(ult_projeceate_defact = cr       projeロジェクトを作成
    # 2. プ
     
        ettings)settings(se_manager.savttings_f.se   sel          )
 
  inbow"alette="Raor_pault_col def          
 ),00, 2ge_size=(200ault_imadef          =100,
  terationslt_max_iauef           dettings(
 ings = AppSsett       . 設定を初期化
       # 1      
    
ストを実行中...")ーテワークフロンデルブロ集合  🔄 完全なマint("    pr""
    ロ集合ワークフロー""完全なマンデルブ        ""
low(self):rot_workf_mandelbete_compl   def test
    
 True)_errors=ignorer, p_dielf.tem(stil.rmtree       shut shutil
   impor
      ""理"の後処  """各テストf):
      n(sel tearDow 
    defgine()
   gEn = Renderining_engine self.render      Manager()
 ColorSystem = olor_managerself.c
            )son")
    ings.jett"test_stemp_dir, (self.path.join    os.       nager(
 SettingsMaer = gs_managttin  self.se     
 _dir)empr(self.tagetManrojecger = P_manactjeself.pro
        p()mkdtemmpfile._dir = te self.temp      
 """各テストの前準備"      ""p(self):
      def setU
    
instance()tion. QApplicapp =s.a        cl   lse:
         es.argv)
on(sy= QApplicati    cls.app        ():
 ncetion.instaicappl if not QA      化"""
 ラス全体の初期""テストク     "cls):
   pClass(tUf se   dessmethod
 @cla   
  
   ""のワークフローテスト""エンドツーエンド"  "Case):
  est.Testow(unittrkflndToEndWotEclass Tes


化統合テスト成功")"  ✅ UI状態永続t(  prin    
      0)
    vel'), 5.oom_le_state('z_uietontroller.g.ui_cl(selfEquaassert      self.5.0)
  el', e('zoom_levtatet_ui_sontroller.sself.ui_c
        新をテストの状態更       # 個別        
 ed_value)
xpectvalue, el(actual_ertEqua  self.ass        key)
  te(r.get_ui_stalleui_controelf._value = s   actual:
         tems().i_stateestue in tpected_valey, ex       for kことを確認
 存される   # 状態が正しく保   
     
     (test_state)ui_statepdate_controller.uself.ui_
                   }
   
  lseel': Faters_pan_parame 'show        
   re',alette': 'fir_p      'colo   0),
   t': (100, 5fse_of   'pan         .5,
l': 2oom_leve   'z  
       (1200, 900),size': ow_     'wind      state = {
       test_状態を設定
  UI  # 
            ")
  実行中...トを永続化統合テスI状態 🔄 Uint("         pr"""
スト統合テUI状態永続化     """   self):
on(gratie_intetencrsisate_pef test_ui_st   
    de合テスト成功")
 "  ✅ 計算結果統(intpr      
        0)
   time,culation_result.calored_(steatersertGr self.as      0, 100))
 .shape, (10data.iteration_ed_resulttorual(sssertEqself.a       
 ult)es_rNone(storedNotrtIsself.asse    
    sult()ller.get_recontrofractal_lf.esult = se stored_r
       とを確認保存されるこ # 結果が正しく        
   sult)
    _result(rentroller.setal_co.fractself     設定
    結果をコントローラーに
        #
        ams)ulate(parlcor.caeratult = genes
        rクタルを計算 フラ       # 
            )
}
    arameters={ustom_p          c),
  e=(100, 100   image_siz   
      0,s=5tionera  max_it
                    ),
  , -1.0)(1.0plexNumber=Comightom_r  bott            , 1.0),
  umber(-2.0omplexNop_left=C          t      n(
ioxRegCompleregion=            rameters(
talPas = Frac       paramータを設定
     # パラメ    
     ()
   eneratortGndelbrorator = Maene        gを作成
タル生成器      # フラク
  
        )中..."合テストを実行 計算結果統 🔄   print(" ""
     結果統合テスト"計算   """):
     n(selfratioult_integtion_resulaest_calc   def t)
    
 タ更新統合テスト成功""  ✅ パラメーint(pr      
     
     aram'], 42)test_prs['paramete.custom_nal_paramsqual(fiself.assertE     )
   800, 600)size, (e_magnal_params.ifiual(rtEqf.assesel       ons, 200)
 .max_iterati_paramsfinaltEqual(ser   self.as     s()
metert_pararoller.getal_cont= self.frac_params      finalラメータを確認
      # 更新されたパ  
          )
 ed_paramspdateters(ur.set_param_controlleractal     self.f       
         )
 }
  m': 42test_para={'ameters  custom_par         00),
 00, 6ize=(8ge_s         ima   =200,
tionsiteramax_    
             ),     
  .5)ber(0.5, -0lexNum_right=Comp bottom               ,
5)0.ber(-1.5, t=ComplexNum     top_lef          xRegion(
 le region=Comp         (
  tersmealParas = Fractram  updated_paを更新
      メータ    # パラ     
    
   00))400, 4ize, (e_smagrams.itored_paal(sssertEqu self.a     
  tions, 100)ax_itera.maramsd_pqual(storertEelf.asse   s
     ed_params)one(storNotNelf.assertIs
        sarameters()r.get_pllerol_cont.fractaself_params = ored      st
  定されることを確認く設正し # パラメータが            
 )
  _paramsters(initialarameset_pler.rolfractal_cont self.
       にパラメータを設定ローラーコント # フラクタル      
 
        
        )meters={}para  custom_      00),
    (400, 4e_size=  imag      =100,
    onsmax_iterati          ),
           -1.0)
   .0, er(1umbght=ComplexNttom_ri       bo         0),
1.r(-2.0, plexNumbeeft=Com      top_l  
        n(gioon=ComplexRe        regieters(
    actalParam = Frrams_paalniti i     
  ラメータを設定 # 初期パ           
 
   )実行中..."ータ更新統合テストをパラメrint("  🔄       p"
  の統合テスト""更新"パラメータ ""      self):
 ntegration(update_iarameter__pef test    
    d")
変更統合テスト成功✅ フラクタルタイプrint("  
        p)tal_typess, fracived_signalrecessertEqual(elf.a    s認
    たことを確グナルが受信され# 全てのシ
        )
        type fractal_rrent_type,ual(cusertEq     self.as   )
        type'ent_fractal_e('currget_ui_stattroller.onself.ui_c_type =      current           ることを確認
状態が正しく更新され   #                      
       
 pe)l_tyype', fractal_tctaent_fratate('curret_ui_sr.slerolntelf.ui_co      s          ーの状態を更新
 # UIコントローラ                
              pe)
 tal_tymit(frachanged.e_cctal_typen_window.fra  self.mai         
     発行    # シグナルを            l_type):
type=fracta(fractal_.subTestith self   w         es:
typfractal_e in  fractal_typ        for       
tom"]
 usia", "c"julrot", elb ["mandctal_types = fra
       をテスト各フラクタルタイプ  #         
    ged)
  l_type_chanacta(on_fr.connectngedype_charactal_twindow.fself.main_ 
        
       type)(fractal_als.appended_sign  receiv
          l_type):d(fractatype_changetal_f on_frac      de  
  = []
      als _sign received       受信をテスト
ナル   # シグ     
     .")
   合テストを実行中..フラクタルタイプ変更統("  🔄      print  "
 合テスト""ルタイプ変更の統ラクタ"フ ""
       f):elation(sange_integrctal_type_chtest_fradef      
  s=True)
 re_errorgno itemp_dir,ree(self..rmtshutil
        t shutilmpor
        i.close()n_windowlf.mai         se   ndow'):
f, 'main_wiasattr(selif h"
        テストの後処理"""各      ""self):
  ef tearDown(
    dize()
    nitialoller.ilf.ui_contr        see()
liziaroller.init_contf.fractal  sel)
      alize(initi_controller.f.main   sel
     を初期化 # コントローラー           
)
    ller( = UIControtroller.ui_con  selfr()
      lleractalControer = Ftrollactal_conf.fr sel  )
     oller(MainContr= ller main_contro      self.
  dow() = MainWindowin.main_w self      kdtemp()
  tempfile.m_dir =  self.temp""
      ""各テストの前準備      ""self):
    def setUp(
    
  ()ance.instonlicatiapp = QApps.          cl  else:
      s.argv)
  lication(sy = QApppp cls.a          tance():
 inson.ticaf not QAppli   i"
     ""体の初期化"テストクラス全 "":
       lass(cls) setUpC
    defethod    @classm"
    
スト""-モデル間の統合テ"UI
    ""se):t.TestCa(unittesntegrationModelIestUI

class Tlt
)
talResuraclexNumber, Fn, CompomplexRegio, Crameters   FractalPaimport (
 models a_ls.dat_editor.modem fractalr
froageettingsManpSettings, S Apmporttings is.app_seteditor.modelctal_
from fraectult_projdefa create_ger,ojectManaimport Pranager .project_mtor.servicesdiactal_es
from frenderSettinge, REnginndering import Rege_renderervices.imator.seredifractal_from per
rMapdientColoanager, GramMt ColorSysteorimpm .color_systetor.servicesedim fractal_or
froratFormulaGene Customporta imultom_formators.cusgenerditor.om fractal_eenerator
fruliaGport Jim.julia rstotor.generafractal_edior
from brotGeneratmport Mandelandelbrot inerators.mgel_editor.fractam ler
frotrololler, UIConactalContrFrller, nControport Maiers.base imcontrollctal_editor.
from fradowinrt MainWimpon_window ui.mail_editor.ctafrom fraをインポート
ポーネントエディタのコン
# フラクタル
(__file__)))pathbsath.a(os.path.dirname.p, ost(0inser
sys.path.トリをパスに追加ートディレクロジェクトのル

# プort Imagefrom PIL impTest
import Q6.QtTest 
from PyQt QEventLooper, QTimrt.QtCore impoQt6
from Pypplication QArtimpoets 6.QtWidgm PyQtock
fro MagicMh,t Mock, patcport.mock imnittesm uronp
fy as  numpimporting
threadt ime
import t
impormpfileort tert os
imprt sys
impot
impoort unittes""

imp"件の統合検証
実装する
- 全要マンステストを パフォー成する
-のワークフローテストを作
- エンドツーエンド統合テストを実装するモデル間の1に対応:
- UI-合検証

要件13.
- 全要件の統パフォーマンステストーテスト  
- フロンドツーエンドのワーク エ間の統合テスト
-:
- UI-モデル合テストを実装しますのファイルは以下の統
こタの包括的統合テスト
 フラクタルエディストスイート -""
統合テ"